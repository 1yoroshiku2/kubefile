   kind: Role
   apiVersion: rbac.authorization.k8s.io/v1
   metadata:
     name: postman-role
     namespace: default
   rules:
   - apiGroups: [""]
     resources: ["pods", "services"]
     verbs: ["create"]
